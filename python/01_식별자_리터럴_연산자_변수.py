'''
    # 식별자(identifier)
        ===> 파이썬이 인지하고 있는 단어들 / 모르는 단어를 사용할 경우 에러 발생
'''

# 식별자는 시스템 식별자와 사용자 식별자로 나뉜다.
'''
    # 시스템 식별자 : 시스템의 필요에 의해서 정의된 단어
        ===> 키워드, 예약어

    # 사용자 식별자 : 사용자의 필요에 의해서 정의된 단어
        ===> 변수명, 함수명, 클래스명
'''

# 리터럴은 값,데이터 / 연산자는 ==, + 와 같은 것

# 코드는 식별자, 리터럴, 연산자로 이루어져있다.

# 변수
'''
    # 파이썬의 모든 변수는 참조형 변수이다.

    # 참조형 변수 : 변수에 가면 실제값이 저장되어 있지 않고
                 실제값이 저장된 주소값을 갖는다.

    # 주소값은 id()함수를 사용
'''